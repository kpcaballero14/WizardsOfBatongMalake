package system;

import java.util.ArrayList;
import java.util.List;

import model.Cashier;
import model.Inventory;
import model.InventoryItem;
import model.Manager;
import model.Menu;
import model.MenuItem;
import model.User;

public class RetailSystem {
    private String businessName;
    private String businessLogoPath;
    private List<User> users;
    private Inventory inventory;
    private Menu menu;

    public RetailSystem() {
        this.businessName = "MAGIC SHOP HAHAHAHHHAAAH";
        this.businessLogoPath = "{LOGO_PATH}";
        this.users = new ArrayList<>();
        this.inventory = new Inventory();
        this.menu = new Menu();
    }

    public void initializeDefaultData() {
        users.add(new Manager("kirbym341", "123", "Kirby"));
        users.add(new Cashier("ewan123", "123", "EWAN"));

        InventoryItem item1 = new InventoryItem();
        inventory.addItem("Lizard Tail");
        inventory.addItem("Cyclop's Eye");
        inventory.addItem("Dragon's Tail");
        inventory.addItem("Tears of a Child");
        inventory.addItem("Siren's Heart");
        inventory.addItem("Distilled Water");
        inventory.addItem("Fish");
        inventory.addItem("Siren's Scales");

        MenuItem menuItem1 = new MenuItem();
        menu.addMenuItem("Potion of Regeneration");
        menu.addMenuItem("Potion of Strength");
        menu.addMenuItem("Potion of Water Breathing");
        menu.addMenuItem("Potion of Teleportation");
        menu.addMenuItem("Potion of Love and Atttraction");
        menu.addMenuItem("Magician's Elixir");
    }

    public User login(String username, String password) {
        for (User user : users) {
            if (user.getUsername().equals(username) &&
                user.getPassword().equals(password)) {
                return user;
            }
        }
        return null;
    }

    public String getBusinessName() {
        return businessName;
    }

    public String getBusinessLogoPath() {
        return businessLogoPath;
    }

    public Inventory getInventory() {
        return inventory;
    }

    public Menu getMenu() {
        return menu;
    }
}
